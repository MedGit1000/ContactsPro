{
  "name": "contact-manager",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "backend": "json-server --watch backend/db.json --port 3000"
  },
  "dependencies": {
    "axios": "^1.6.5",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "json-server": "^0.17.4",
    "vite": "^6.3.4"
  }
}
